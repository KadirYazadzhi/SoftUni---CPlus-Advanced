#include <iostream>
#include <sstream>
#include <string>

using namespace std;

int main() {
    int rows, columns;

    cin >> rows;

    string line;
    getline(cin, line);
    istringstream istr(line);

    int num;
    int i = 0;
    while (istr >> num) {
        
    }

}